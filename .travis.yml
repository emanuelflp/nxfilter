sudo: required
services: docker
language: generic

before_deploy:
  - docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD"

deploy:
  provider: script
  script: bash scripts/deploy.sh
  on:
    branch: master
